apiVersion: v1
kind: Service
metadata:
  name: threshold
  labels:
    app.kubernetes.io/name: threshold
spec:
  selector:
    app.kubernetes.io/name: threshold
  ports:
    - name: http
      port: 3000
      targetPort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: threshold
  labels:
    app.kubernetes.io/name: threshold
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: threshold
  template:
    metadata:
      labels:
        app.kubernetes.io/name: threshold
    spec:
      containers:
        - name: threshold
          image: ghcr.io/energyconsumptionoptimizer/threshold-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: eco-config
            - secretRef:
                name: eco-secrets
          env:
            - name: NAME
              value: threshold
            - name: PORT
              value: "3000"
            - name: MONGO_DB
              value: threshold
          startupProbe:
            httpGet:
              path: /health
              port: 3000
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
